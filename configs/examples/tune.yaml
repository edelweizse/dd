optimization:
  timeout: 14400
  quick: true

study:
  study_name: EA-HAN_cd_tuning
  storage: sqlite:///optuna.db
  experiment_name: EA-HAN_tuning
  output_dir: ./tuning_results

data:
  processed_dir: ./data/processed

training:
  epochs_per_trial: 50
  early_stopping: 5
