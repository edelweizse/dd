paths:
  processed_dir: ./data/processed
  checkpoint: /checkpoints/best.pt

graph:
  use_node_features: true
  node_features_dir: ./data/processed/features
  no_extended: false

model:
  hidden_dim: 128
  num_layers: 2
  num_heads: 4
  dropout: 0.2

query:
  disease: null
  chemical: C006901
  top_k: 20
  include_known: false
  threshold: 0.5
